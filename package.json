{
    "homepage": "https://joeyshea.github.io/",
    "devDependencies": {
        "gh-pages": "^6.3.0"
    },
    "scripts": {
        "build": "vite build",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist"
    }
}
